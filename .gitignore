# Dependencies
node_modules/

# Build outputs
dist/
# Do not use a bare `lib/` here — it ignores every folder named `lib` (e.g. `apps/pwa/src/lib/`).
/lib/
peer-support-app/lib/
lib-dts/
lib-commonjs/
lib-esm/
release/
solution/
temp/
jest-output/
.heft/

# SharePoint package
*.sppkg

# Logs and coverage
logs/
*.log
npm-debug.log*
coverage/

# Local env (never commit secrets)
.env
.env.local
.env.*.local

# Ad-hoc Ably key text files at repo root (never commit)
Ably_API_Ket7
Ably_API_Ket7.txt
Ably_API_Key
Ably_API_Key.txt
Root_API.txt

# Cloudflare API token at repo root (never commit)
CloudflareToken
CloudflareToken.txt
CloudflareAccountId.txt

# OS / IDE
.DS_Store
Thumbs.db
.ntvs_analysis.dat
.vs/
*.user

# Generated (SPFx)
*.resx.ts
*.scss.ts
